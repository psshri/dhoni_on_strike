CREATE TABLE fixtures_2023 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    date DATE,
    match_id INT,
    match_subtitle VARCHAR(255),
    home_team VARCHAR(255),
    away_team VARCHAR(255),
    result VARCHAR(255),
    status VARCHAR(255)
);
